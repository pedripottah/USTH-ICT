#include <stdio.h>

int main() {
    // arithmetic operators ( = + - / % ++ --)

    int x = 10;
    float y = 3;
    float z = 1;

    // z = x + y;
    // z = x - y;
    // z = x * y;
    // z = x / y;
    // z = x % 3;

    // x++;
    // x--;
    // x += 2;
    // x -= 2;

    printf("%d\n", x);
    printf("%.2f", z);
}